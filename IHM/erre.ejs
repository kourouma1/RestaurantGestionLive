<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>404 - Page Non Trouvée</title>
  <!-- Bootstrap CSS -->
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <!-- Style personnalisé -->
  <style>
    body, html {
      height: 100%;
      margin: 0;
      font-family: 'Arial', sans-serif;
      overflow: hidden;
    }

    .video-background {
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      z-index: -1;
      object-fit: cover;
    }

    .content {
      position: relative;
      z-index: 1;
      height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
      text-align: center;
      color: white;
    }

    h1 {
      font-size: 8rem;
      font-weight: bold;
      animation: rotateAndScale 4s infinite ease-in-out;
    }

    p {
      font-size: 1.5rem;
      margin-top: 20px;
    }

    .btn-home {
      margin-top: 30px;
      padding: 10px 30px;
      font-size: 1.2rem;
      animation: pulse 2s infinite;
    }

    /* Animation pour le titre "404" */
    @keyframes rotateAndScale {
      0% {
        transform: rotate(0deg) scale(1);
      }
      25% {
        transform: rotate(5deg) scale(1.1);
      }
      50% {
        transform: rotate(0deg) scale(1);
      }
      75% {
        transform: rotate(-5deg) scale(1.1);
      }
      100% {
        transform: rotate(0deg) scale(1);
      }
    }

    /* Animation pour le bouton */
    @keyframes pulse {
      0% {
        transform: scale(1);
      }
      50% {
        transform: scale(1.1);
      }
      100% {
        transform: scale(1);
      }
    }
  </style>
</head>
<body>
  <!-- Vidéo de fond -->
  <video class="video-background" autoplay muted loop>
    <source src="../assets/video/pub (1).mp4" type="video/mp4">
    Votre navigateur ne supporte pas la vidéo.
  </video>

  <!-- Contenu de la page 404 -->
  <div class="content">
    <div>
      <h1>404</h1>
      <p>Oups ! La page que vous cherchez n'existe pas.</p>
      <a href="/page/" class="btn btn-warning btn-close-white btn-home">Retour à l'accueil</a>
    </div>
  </div>

  <!-- Bootstrap JavaScript (nécessaire pour les composants interactifs comme la navbar, les modals, etc.) -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>